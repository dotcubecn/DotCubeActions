name: Quick Reference
on:
  schedule:
    - cron: '0 * * * *' # 每小时00分执行
  workflow_dispatch:    # 手动运行

env: # 设置环境变量
  TZ: Asia/Shanghai # 时区

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🚜 拉取最新代码
        uses: actions/checkout@v3
        with:
          ref: 'main'
          repository: 'jaywcjlove/reference'

      - name: 🔧 设置 Node.js 环境 (LTS)
        uses: actions/setup-node@v3
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: ♻️ 编译静态文件
        run: |
          # 获取当前年份
          CURRENT_YEAR=$(date +%Y)
          # 创建.env文件并写入环境变量
          echo "REF_URL=${{ vars.REF_URL || 'http://dotcube.cc/' }}" > .env
          echo "REF_LABEL=${{ vars.REF_LABEL || '主页' }}" >> .env
          echo "LICENSE=© $CURRENT_YEAR <a href=\"https://dotcube.cc/\">DotCube</a>" >> .env
          npm ci
          npm run build

      - name: 🚀 WebDAV 部署到 storage.dotcube.cc
        uses: bramus/webdav-deploy@v2
        with:
          webdav_host: 'https://storage.dotcube.cc/dav'
          webdav_username: ${{ secrets.WEBDAV_USERNAME }}
          webdav_password: ${{ secrets.WEBDAV_PASSWORD }}
          webdav_target_dir: '/'  # 部署到WebDAV根目录
          local_dir: './dist'
          clean: true  # 上传前清除目标目录
